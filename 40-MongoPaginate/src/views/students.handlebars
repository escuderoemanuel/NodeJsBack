<h1>Students</h1>

<ul>
  {{#each students}}
  <li>{{this.first_name}} {{this.last_name}} </li>
  {{/each}}
</ul>

{{#if hasPrevPage}}
<a href="{{prevLink}}">Prev</a>
{{/if}}

{{#if hasNextPage}}
<a href="{{nextLink}}">Next</a>
{{/if}}